source("renv/activate.R")


options(
  tidyverse.quiet = TRUE
)

if (interactive()) {
  suppressPackageStartupMessages(suppressWarnings({
    library(devtools)
    library(usethis)
    library(checkmate)
    library(testthat)
  }))
}
